jwt:
  secret: '[a-zA-Z0-9._]^+$Guidelines897979forAlphabeticalArraNumeralsandOtherSymbo$'

server:
  port: 8081
  #error:
    #whitelabel:
      #enabled: false

spring:
  datasource:
    username: chids_p
    password: Harrier#0252
    url: jdbc:mysql://localhost:3306/portal_db?createDatabaseIfNotExist=true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  #mvc:
    #throw-exception-if-no-handler-found: true
  #resources:
    #add-mappings: false
  serve:
    error:
      path: /error
